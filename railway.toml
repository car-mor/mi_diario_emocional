[build]
builder = "DOCKERFILE"
dockerfilePath = "backend/Dockerfile"  # ← Ruta COMPLETA desde la raíz
# NO pongas buildContext

[deploy]
startCommand = "gunicorn backend.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120"